apiVersion: v1
kind: Service
metadata:
  name: promtail
  namespace: nitte-dev
  labels:
    app: promtail
spec:
  selector:
    app: promtail
  ports:
  - name: http-metrics
    port: 9080
    targetPort: 9080
