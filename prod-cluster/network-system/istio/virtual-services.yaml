# VirtualServices for Prod cluster — host-based routing
---
# Storefront (+ shared API / auth / grafana)
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: storefront-routes
spec:
  hosts:
    - "prod.nitte.local"
  gateways:
    - nitte-gateway
  http:
    - match:
        - uri: { prefix: /api }
      route:
        - destination: { host: node-backend, port: { number: 3000 } }
      timeout: 30s
      retries:
        attempts: 3
        perTryTimeout: 10s
        retryOn: gateway-error,connect-failure,refused-stream
    - match:
        - uri: { prefix: /auth }
        - uri: { prefix: /realms }
      route:
        - destination: { host: keycloak, port: { number: 8080 } }
    - match:
        - uri: { prefix: /grafana }
      route:
        - destination: { host: grafana, port: { number: 3000 } }
    - match:
        - uri: { prefix: / }
      route:
        - destination: { host: frontend, port: { number: 5173 } }
---
# Admin dashboard
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: admin-routes
spec:
  hosts:
    - "admin.prod.nitte.local"
  gateways:
    - nitte-gateway
  http:
    - match:
        - uri: { prefix: /api }
      route:
        - destination: { host: node-backend, port: { number: 3000 } }
    - match:
        - uri: { prefix: /auth }
        - uri: { prefix: /realms }
      route:
        - destination: { host: keycloak, port: { number: 8080 } }
    - match:
        - uri: { prefix: / }
      route:
        - destination: { host: admin-dashboard, port: { number: 5174 } }
---
# Merchant portal
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: merchant-routes
spec:
  hosts:
    - "merchant.prod.nitte.local"
  gateways:
    - nitte-gateway
  http:
    - match:
        - uri: { prefix: /api }
      route:
        - destination: { host: node-backend, port: { number: 3000 } }
    - match:
        - uri: { prefix: /auth }
        - uri: { prefix: /realms }
      route:
        - destination: { host: keycloak, port: { number: 8080 } }
    - match:
        - uri: { prefix: / }
      route:
        - destination: { host: merchant-portal, port: { number: 5175 } }
---
# Keycloak UI
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: keycloak-routes
spec:
  hosts:
    - "keycloak.prod.nitte.local"
  gateways:
    - nitte-gateway
  http:
    - route:
        - destination: { host: keycloak, port: { number: 8080 } }
---
# Grafana UI
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: grafana-routes
spec:
  hosts:
    - "grafana.prod.nitte.local"
  gateways:
    - nitte-gateway
  http:
    - route:
        - destination: { host: grafana, port: { number: 3000 } }
---
# Prometheus UI
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: prometheus-routes
spec:
  hosts:
    - "prometheus.prod.nitte.local"
  gateways:
    - nitte-gateway
  http:
    - route:
        - destination: { host: prometheus-agent, port: { number: 9090 } }
---
# Jaeger UI
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: jaeger-routes
spec:
  hosts:
    - "jaeger.prod.nitte.local"
  gateways:
    - nitte-gateway
  http:
    - route:
        - destination: { host: jaeger, port: { number: 16686 } }
---
# MinIO Console
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: minio-routes
spec:
  hosts:
    - "minio.prod.nitte.local"
  gateways:
    - nitte-gateway
  http:
    - route:
        - destination: { host: minio, port: { number: 9001 } }
